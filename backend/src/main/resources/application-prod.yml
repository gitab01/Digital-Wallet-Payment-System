spring:
  datasource:
    url: ${DATABASE_URL}
    hikari:
      maximum-pool-size: 3
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    root: WARN
    com.digitalwallet: INFO
